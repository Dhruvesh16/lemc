defaults: [default]

dataset: pie
track_store_file: pie_track_store_augmented.pkl
orb_affines_file: pie_orb_affines.pkl
run_tag: augmented_orb_affine
use_lemc: true
max_agents: 32

lemc:
  hidden_dim: 32
  temporal_net: gru

backbone:
  hidden_dim: 128
  num_layers: 2
  predict_intent: true

train:
  batch_size: 64
  lr: 1.0e-3
  epochs: 40
  early_stop_patience: 20
  overlap_train: 0.5

loss:
  traj_weight: 1.0
  intent_weight: 1.0
  residual_l2_penalty: 1.0e-4
  orb_weight: 2.0  # affine-induced ego-centre shift (not global translation)
  obd_weight: 0.0
